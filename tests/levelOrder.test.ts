import { levelOrder } from '../src/levelOrder';
import { describe, it, expect } from 'vitest';

describe('levelOrder', () => {
  it('should pass', () => {
    expect(levelOrder()).toBeUndefined();
  });
});
