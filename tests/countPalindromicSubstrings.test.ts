import { countPalindromicSubstrings } from '../src/countPalindromicSubstrings';
import { describe, it, expect } from 'vitest';

describe('countPalindromicSubstrings', () => {
  it('should pass', () => {
    expect(countPalindromicSubstrings()).toBeUndefined();
  });
});
